/**
 * Launch Tower (/launches) — the three socials (Substack, X, LinkedIn) from
 * about.js.
 */
import { about } from "../about";

export const launchesAttraction = {
  id: "launches",
  route: "/launches",
  title: "Launch Tower",
  tagline: "signals out to the world.",
  socials: about.socials,
};
