/**
 * Hardware Workshop (/hardware) — the user manual: the six-bullet bio, the
 * principles, and the story line.
 */
import { userManual } from "../userManual";

export const hardwareAttraction = {
  id: "hardware",
  route: "/hardware",
  title: "Hardware Workshop",
  tagline: "the operator's manual.",
  manual: userManual,
};
